pkg=rust
ver=1.17.0
rel=1
sum="Systems programming language focused on safety, speed and concurrency"
lic="APACHE, MIT"
url=https://www.rust-lang.org
dep=(gcc)
mkd=(libffi perl python curl cmake)
opt=(!strip !emptydirs)
src=(http://static.rust-lang.org/dist/rustc-$ver-src.tar.gz)
sha=(4baba3895b75f2492df6ce5a28a916307ecd1c088dc1fd02dbfa8a8e86174f87)

build() {
    ./configure --prefix=/usr \
                --release-channel=stable \
                --enable-local-rust

    python ./x.py build --verbose
}

package() {
    make DESTDIR=$pkgdir install

    cd $pkgdir/usr/lib
    rm rustlib/{components,manifest-rustc,rust-installer-version}
    ln -sf rustlib/$CARCH-unknown-linux-gnu/lib/*.so .
}
